# Algorithm Design and Analysis Projects

## Lab 1

To run, cd to the root directory of the project and run:

```sh
pip install -r DupIdentification/requirements.txt
python DupIdentification/main.py
```
